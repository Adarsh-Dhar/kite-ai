# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class MarketActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Market]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Market.prisma().query_raw(
            'SELECT * FROM Market WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Market
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Market.prisma().query_first(
            'SELECT * FROM Market WHERE onchainMarketId = $1',
            377401575,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.MarketCreateInput,
        include: Optional[types.MarketInclude] = None
    ) -> _PrismaModelT:
        """Create a new Market record.

        Parameters
        ----------
        data
            Market record data
        include
            Specifies which relations should be loaded on the returned Market model

        Returns
        -------
        prisma.models.Market
            The created Market record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Market record from just the required fields
        market = await Market.prisma().create(
            data={
                # data to create a Market record
                'title': 'ggciceaie',
                'question': 'bbehjachib',
                'category': 'cadfabfehe',
                'agentReason': 'dgiiaaijj',
                'resolutionType': enums.ResolutionType.GITHUB_PR,
                'dataSourceUrl': 'bfaiacjjfc',
                'evaluationLogic': Json({'eigcfgbif': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.MarketCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Market records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Market record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Market.prisma().create_many(
            data=[
                {
                    # data to create a Market record
                    'title': 'bagcfbhiig',
                    'question': 'cghideieh',
                    'category': 'biabhbdai',
                    'agentReason': 'idghgaicb',
                    'resolutionType': enums.ResolutionType.GITHUB_PR,
                    'dataSourceUrl': 'fjfddhigg',
                    'evaluationLogic': Json({'hjaecfifb': True}),
                },
                {
                    # data to create a Market record
                    'title': 'cbbbjbfcii',
                    'question': 'bbejhfidcb',
                    'category': 'bgeecijdgg',
                    'agentReason': 'bdiicjafbj',
                    'resolutionType': enums.ResolutionType.GITHUB_PR,
                    'dataSourceUrl': 'bgehebiafc',
                    'evaluationLogic': Json({'bghffegacj': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.MarketWhereUniqueInput,
        include: Optional[types.MarketInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Market record.

        Parameters
        ----------
        where
            Market filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Market model

        Returns
        -------
        prisma.models.Market
            The deleted Market record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        market = await Market.prisma().delete(
            where={
                'id': 'bhghchehcc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.MarketWhereUniqueInput,
        include: Optional[types.MarketInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Market record.

        Parameters
        ----------
        where
            Market filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Market model

        Returns
        -------
        prisma.models.Market
            The found Market record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        market = await Market.prisma().find_unique(
            where={
                'id': 'dcgchcbbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.MarketWhereUniqueInput,
        include: Optional[types.MarketInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Market record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Market filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Market model

        Returns
        -------
        prisma.models.Market
            The found Market record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        market = await Market.prisma().find_unique_or_raise(
            where={
                'id': 'bdedcabahc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MarketWhereInput] = None,
        cursor: Optional[types.MarketWhereUniqueInput] = None,
        include: Optional[types.MarketInclude] = None,
        order: Optional[Union[types.MarketOrderByInput, List[types.MarketOrderByInput]]] = None,
        distinct: Optional[List[types.MarketScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Market records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Market records returned
        skip
            Ignore the first N results
        where
            Market filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Market model
        order
            Order the returned Market records by any field
        distinct
            Filter Market records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Market]
            The list of all Market records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Market records
        markets = await Market.prisma().find_many(take=10)

        # find the first 5 Market records ordered by the transactionHash field
        markets = await Market.prisma().find_many(
            take=5,
            order={
                'transactionHash': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.MarketWhereInput] = None,
        cursor: Optional[types.MarketWhereUniqueInput] = None,
        include: Optional[types.MarketInclude] = None,
        order: Optional[Union[types.MarketOrderByInput, List[types.MarketOrderByInput]]] = None,
        distinct: Optional[List[types.MarketScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Market record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Market filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Market model
        order
            Order the returned Market records by any field
        distinct
            Filter Market records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Market
            The first Market record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Market record ordered by the blockNumber field
        market = await Market.prisma().find_first(
            skip=1,
            order={
                'blockNumber': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.MarketWhereInput] = None,
        cursor: Optional[types.MarketWhereUniqueInput] = None,
        include: Optional[types.MarketInclude] = None,
        order: Optional[Union[types.MarketOrderByInput, List[types.MarketOrderByInput]]] = None,
        distinct: Optional[List[types.MarketScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Market record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Market filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Market model
        order
            Order the returned Market records by any field
        distinct
            Filter Market records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Market
            The first Market record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Market record ordered by the contractAddress field
        market = await Market.prisma().find_first_or_raise(
            skip=1,
            order={
                'contractAddress': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.MarketUpdateInput,
        where: types.MarketWhereUniqueInput,
        include: Optional[types.MarketInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Market record.

        Parameters
        ----------
        data
            Market record data specifying what to update
        where
            Market filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Market model

        Returns
        -------
        prisma.models.Market
            The updated Market record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        market = await Market.prisma().update(
            where={
                'id': 'ghfhiafcb',
            },
            data={
                # data to update the Market record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.MarketWhereUniqueInput,
        data: types.MarketUpsertInput,
        include: Optional[types.MarketInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Market filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Market model

        Returns
        -------
        prisma.models.Market
            The created or updated Market record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        market = await Market.prisma().upsert(
            where={
                'id': 'heejgedji',
            },
            data={
                'create': {
                    'id': 'heejgedji',
                    'title': 'cbbbjbfcii',
                    'question': 'bbejhfidcb',
                    'category': 'bgeecijdgg',
                    'agentReason': 'bdiicjafbj',
                    'resolutionType': enums.ResolutionType.GITHUB_PR,
                    'dataSourceUrl': 'bgehebiafc',
                    'evaluationLogic': Json({'bghffegacj': True}),
                },
                'update': {
                    'title': 'cbbbjbfcii',
                    'question': 'bbejhfidcb',
                    'category': 'bgeecijdgg',
                    'agentReason': 'bdiicjafbj',
                    'resolutionType': enums.ResolutionType.GITHUB_PR,
                    'dataSourceUrl': 'bgehebiafc',
                    'evaluationLogic': Json({'bghffegacj': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.MarketUpdateManyMutationInput,
        where: types.MarketWhereInput,
    ) -> int:
        """Update multiple Market records

        Parameters
        ----------
        data
            Market data to update the selected Market records to
        where
            Filter to select the Market records to update

        Returns
        -------
        int
            The total number of Market records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Market records
        total = await Market.prisma().update_many(
            data={
                'title': 'bjgjgibgbf'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MarketWhereInput] = None,
        cursor: Optional[types.MarketWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Market records present in the database

        Parameters
        ----------
        select
            Select the Market fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Market filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.MarketCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Market.prisma().count()

        # results: prisma.types.MarketCountAggregateOutput
        results = await Market.prisma().count(
            select={
                '_all': True,
                'question': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.MarketCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MarketWhereInput] = None,
        cursor: Optional[types.MarketWhereUniqueInput] = None,
    ) -> types.MarketCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.MarketCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.MarketWhereInput] = None,
        cursor: Optional[types.MarketWhereUniqueInput] = None,
    ) -> Union[int, types.MarketCountAggregateOutput]:
        """Count the number of Market records present in the database

        Parameters
        ----------
        select
            Select the Market fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Market filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.MarketCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Market.prisma().count()

        # results: prisma.types.MarketCountAggregateOutput
        results = await Market.prisma().count(
            select={
                '_all': True,
                'category': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.MarketCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.MarketWhereInput] = None
    ) -> int:
        """Delete multiple Market records.

        Parameters
        ----------
        where
            Optional Market filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Market records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Market records
        total = await Market.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.MarketScalarFieldKeys'],
        *,
        where: Optional['types.MarketWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.MarketAvgAggregateInput'] = None,
        sum: Optional['types.MarketSumAggregateInput'] = None,
        min: Optional['types.MarketMinAggregateInput'] = None,
        max: Optional['types.MarketMaxAggregateInput'] = None,
        having: Optional['types.MarketScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.MarketCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.MarketScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.MarketScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.MarketGroupByOutput']:
        """Group Market records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Market fields to group records by
        where
            Market filter to select records
        take
            Limit the maximum number of Market records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.MarketGroupByOutput]
            A list of dictionaries representing the Market record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Market records by options values
        # and count how many records are in each group
        results = await Market.prisma().group_by(
            ['options'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DeployedPRActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.DeployedPR]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await DeployedPR.prisma().query_raw(
            'SELECT * FROM DeployedPR WHERE id = $1',
            'bbbgbhfjge',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.DeployedPR
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await DeployedPR.prisma().query_first(
            'SELECT * FROM DeployedPR WHERE prNumber = $1',
            861472101,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.DeployedPRCreateInput,
        include: Optional[types.DeployedPRInclude] = None
    ) -> _PrismaModelT:
        """Create a new DeployedPR record.

        Parameters
        ----------
        data
            DeployedPR record data
        include
            Specifies which relations should be loaded on the returned DeployedPR model

        Returns
        -------
        prisma.models.DeployedPR
            The created DeployedPR record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a DeployedPR record from just the required fields
        deployedpr = await DeployedPR.prisma().create(
            data={
                # data to create a DeployedPR record
                'prNumber': 1303003706,
                'prTitle': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.DeployedPRCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple DeployedPR records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of DeployedPR record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await DeployedPR.prisma().create_many(
            data=[
                {
                    # data to create a DeployedPR record
                    'prNumber': 2000430152,
                    'prTitle': 'bigibebcib',
                },
                {
                    # data to create a DeployedPR record
                    'prNumber': 1860847622,
                    'prTitle': 'beeifcbebf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.DeployedPRWhereUniqueInput,
        include: Optional[types.DeployedPRInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single DeployedPR record.

        Parameters
        ----------
        where
            DeployedPR filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned DeployedPR model

        Returns
        -------
        prisma.models.DeployedPR
            The deleted DeployedPR record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployedpr = await DeployedPR.prisma().delete(
            where={
                'id': 'bgcigfahea',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.DeployedPRWhereUniqueInput,
        include: Optional[types.DeployedPRInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique DeployedPR record.

        Parameters
        ----------
        where
            DeployedPR filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DeployedPR model

        Returns
        -------
        prisma.models.DeployedPR
            The found DeployedPR record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployedpr = await DeployedPR.prisma().find_unique(
            where={
                'id': 'bcejgaggif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.DeployedPRWhereUniqueInput,
        include: Optional[types.DeployedPRInclude] = None
    ) -> _PrismaModelT:
        """Find a unique DeployedPR record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            DeployedPR filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DeployedPR model

        Returns
        -------
        prisma.models.DeployedPR
            The found DeployedPR record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployedpr = await DeployedPR.prisma().find_unique_or_raise(
            where={
                'id': 'idfjadbcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeployedPRWhereInput] = None,
        cursor: Optional[types.DeployedPRWhereUniqueInput] = None,
        include: Optional[types.DeployedPRInclude] = None,
        order: Optional[Union[types.DeployedPROrderByInput, List[types.DeployedPROrderByInput]]] = None,
        distinct: Optional[List[types.DeployedPRScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple DeployedPR records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of DeployedPR records returned
        skip
            Ignore the first N results
        where
            DeployedPR filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DeployedPR model
        order
            Order the returned DeployedPR records by any field
        distinct
            Filter DeployedPR records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.DeployedPR]
            The list of all DeployedPR records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 DeployedPR records
        deployedprs = await DeployedPR.prisma().find_many(take=10)

        # find the first 5 DeployedPR records ordered by the prTitle field
        deployedprs = await DeployedPR.prisma().find_many(
            take=5,
            order={
                'prTitle': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeployedPRWhereInput] = None,
        cursor: Optional[types.DeployedPRWhereUniqueInput] = None,
        include: Optional[types.DeployedPRInclude] = None,
        order: Optional[Union[types.DeployedPROrderByInput, List[types.DeployedPROrderByInput]]] = None,
        distinct: Optional[List[types.DeployedPRScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single DeployedPR record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DeployedPR filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DeployedPR model
        order
            Order the returned DeployedPR records by any field
        distinct
            Filter DeployedPR records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DeployedPR
            The first DeployedPR record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DeployedPR record ordered by the prUrl field
        deployedpr = await DeployedPR.prisma().find_first(
            skip=1,
            order={
                'prUrl': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DeployedPRWhereInput] = None,
        cursor: Optional[types.DeployedPRWhereUniqueInput] = None,
        include: Optional[types.DeployedPRInclude] = None,
        order: Optional[Union[types.DeployedPROrderByInput, List[types.DeployedPROrderByInput]]] = None,
        distinct: Optional[List[types.DeployedPRScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single DeployedPR record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DeployedPR filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DeployedPR model
        order
            Order the returned DeployedPR records by any field
        distinct
            Filter DeployedPR records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DeployedPR
            The first DeployedPR record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DeployedPR record ordered by the mergedAt field
        deployedpr = await DeployedPR.prisma().find_first_or_raise(
            skip=1,
            order={
                'mergedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.DeployedPRUpdateInput,
        where: types.DeployedPRWhereUniqueInput,
        include: Optional[types.DeployedPRInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single DeployedPR record.

        Parameters
        ----------
        data
            DeployedPR record data specifying what to update
        where
            DeployedPR filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned DeployedPR model

        Returns
        -------
        prisma.models.DeployedPR
            The updated DeployedPR record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        deployedpr = await DeployedPR.prisma().update(
            where={
                'id': 'hgdhbjhhj',
            },
            data={
                # data to update the DeployedPR record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.DeployedPRWhereUniqueInput,
        data: types.DeployedPRUpsertInput,
        include: Optional[types.DeployedPRInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            DeployedPR filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned DeployedPR model

        Returns
        -------
        prisma.models.DeployedPR
            The created or updated DeployedPR record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        deployedpr = await DeployedPR.prisma().upsert(
            where={
                'id': 'ecjjjfbae',
            },
            data={
                'create': {
                    'id': 'ecjjjfbae',
                    'prNumber': 1860847622,
                    'prTitle': 'beeifcbebf',
                },
                'update': {
                    'prNumber': 1860847622,
                    'prTitle': 'beeifcbebf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.DeployedPRUpdateManyMutationInput,
        where: types.DeployedPRWhereInput,
    ) -> int:
        """Update multiple DeployedPR records

        Parameters
        ----------
        data
            DeployedPR data to update the selected DeployedPR records to
        where
            Filter to select the DeployedPR records to update

        Returns
        -------
        int
            The total number of DeployedPR records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all DeployedPR records
        total = await DeployedPR.prisma().update_many(
            data={
                'tssScore': 1775811865.89314
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeployedPRWhereInput] = None,
        cursor: Optional[types.DeployedPRWhereUniqueInput] = None,
    ) -> int:
        """Count the number of DeployedPR records present in the database

        Parameters
        ----------
        select
            Select the DeployedPR fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DeployedPR filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeployedPRCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DeployedPR.prisma().count()

        # results: prisma.types.DeployedPRCountAggregateOutput
        results = await DeployedPR.prisma().count(
            select={
                '_all': True,
                'deployedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.DeployedPRCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeployedPRWhereInput] = None,
        cursor: Optional[types.DeployedPRWhereUniqueInput] = None,
    ) -> types.DeployedPRCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.DeployedPRCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DeployedPRWhereInput] = None,
        cursor: Optional[types.DeployedPRWhereUniqueInput] = None,
    ) -> Union[int, types.DeployedPRCountAggregateOutput]:
        """Count the number of DeployedPR records present in the database

        Parameters
        ----------
        select
            Select the DeployedPR fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DeployedPR filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DeployedPRCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DeployedPR.prisma().count()

        # results: prisma.types.DeployedPRCountAggregateOutput
        results = await DeployedPR.prisma().count(
            select={
                '_all': True,
                'marketId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DeployedPRCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.DeployedPRWhereInput] = None
    ) -> int:
        """Delete multiple DeployedPR records.

        Parameters
        ----------
        where
            Optional DeployedPR filter to find the records to be deleted

        Returns
        -------
        int
            The total number of DeployedPR records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all DeployedPR records
        total = await DeployedPR.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.DeployedPRScalarFieldKeys'],
        *,
        where: Optional['types.DeployedPRWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DeployedPRAvgAggregateInput'] = None,
        sum: Optional['types.DeployedPRSumAggregateInput'] = None,
        min: Optional['types.DeployedPRMinAggregateInput'] = None,
        max: Optional['types.DeployedPRMaxAggregateInput'] = None,
        having: Optional['types.DeployedPRScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DeployedPRCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DeployedPRScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DeployedPRScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DeployedPRGroupByOutput']:
        """Group DeployedPR records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar DeployedPR fields to group records by
        where
            DeployedPR filter to select records
        take
            Limit the maximum number of DeployedPR records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DeployedPRGroupByOutput]
            A list of dictionaries representing the DeployedPR record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group DeployedPR records by id values
        # and count how many records are in each group
        results = await DeployedPR.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ResolutionLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ResolutionLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ResolutionLog.prisma().query_raw(
            'SELECT * FROM ResolutionLog WHERE id = $1',
            'jjfeafhfj',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ResolutionLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ResolutionLog.prisma().query_first(
            'SELECT * FROM ResolutionLog WHERE marketId = $1',
            'cbachdgfce',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ResolutionLogCreateInput,
        include: Optional[types.ResolutionLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new ResolutionLog record.

        Parameters
        ----------
        data
            ResolutionLog record data
        include
            Specifies which relations should be loaded on the returned ResolutionLog model

        Returns
        -------
        prisma.models.ResolutionLog
            The created ResolutionLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ResolutionLog record from just the required fields
        resolutionlog = await ResolutionLog.prisma().create(
            data={
                # data to create a ResolutionLog record
                'marketId': 'chbfcacbd',
                'attemptNumber': 456633834,
                'resolverType': 'caficfigfb',
                'decision': 'bfidgijfjc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ResolutionLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ResolutionLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ResolutionLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ResolutionLog.prisma().create_many(
            data=[
                {
                    # data to create a ResolutionLog record
                    'marketId': 'ihieecagf',
                    'attemptNumber': 1675280054,
                    'resolverType': 'bgchfhgceh',
                    'decision': 'cafeiaccbc',
                },
                {
                    # data to create a ResolutionLog record
                    'marketId': 'gaddfhfh',
                    'attemptNumber': 684462146,
                    'resolverType': 'bgcffadich',
                    'decision': 'fcbichhci',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ResolutionLogWhereUniqueInput,
        include: Optional[types.ResolutionLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ResolutionLog record.

        Parameters
        ----------
        where
            ResolutionLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ResolutionLog model

        Returns
        -------
        prisma.models.ResolutionLog
            The deleted ResolutionLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        resolutionlog = await ResolutionLog.prisma().delete(
            where={
                'id': 'bcggadccgf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ResolutionLogWhereUniqueInput,
        include: Optional[types.ResolutionLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ResolutionLog record.

        Parameters
        ----------
        where
            ResolutionLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ResolutionLog model

        Returns
        -------
        prisma.models.ResolutionLog
            The found ResolutionLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        resolutionlog = await ResolutionLog.prisma().find_unique(
            where={
                'id': 'jdcfdcgc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ResolutionLogWhereUniqueInput,
        include: Optional[types.ResolutionLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ResolutionLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ResolutionLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ResolutionLog model

        Returns
        -------
        prisma.models.ResolutionLog
            The found ResolutionLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        resolutionlog = await ResolutionLog.prisma().find_unique_or_raise(
            where={
                'id': 'cafdaehjid',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ResolutionLogWhereInput] = None,
        cursor: Optional[types.ResolutionLogWhereUniqueInput] = None,
        include: Optional[types.ResolutionLogInclude] = None,
        order: Optional[Union[types.ResolutionLogOrderByInput, List[types.ResolutionLogOrderByInput]]] = None,
        distinct: Optional[List[types.ResolutionLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ResolutionLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ResolutionLog records returned
        skip
            Ignore the first N results
        where
            ResolutionLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ResolutionLog model
        order
            Order the returned ResolutionLog records by any field
        distinct
            Filter ResolutionLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ResolutionLog]
            The list of all ResolutionLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ResolutionLog records
        resolutionlogs = await ResolutionLog.prisma().find_many(take=10)

        # find the first 5 ResolutionLog records ordered by the attemptNumber field
        resolutionlogs = await ResolutionLog.prisma().find_many(
            take=5,
            order={
                'attemptNumber': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ResolutionLogWhereInput] = None,
        cursor: Optional[types.ResolutionLogWhereUniqueInput] = None,
        include: Optional[types.ResolutionLogInclude] = None,
        order: Optional[Union[types.ResolutionLogOrderByInput, List[types.ResolutionLogOrderByInput]]] = None,
        distinct: Optional[List[types.ResolutionLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ResolutionLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ResolutionLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ResolutionLog model
        order
            Order the returned ResolutionLog records by any field
        distinct
            Filter ResolutionLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ResolutionLog
            The first ResolutionLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ResolutionLog record ordered by the resolverType field
        resolutionlog = await ResolutionLog.prisma().find_first(
            skip=1,
            order={
                'resolverType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ResolutionLogWhereInput] = None,
        cursor: Optional[types.ResolutionLogWhereUniqueInput] = None,
        include: Optional[types.ResolutionLogInclude] = None,
        order: Optional[Union[types.ResolutionLogOrderByInput, List[types.ResolutionLogOrderByInput]]] = None,
        distinct: Optional[List[types.ResolutionLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ResolutionLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ResolutionLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ResolutionLog model
        order
            Order the returned ResolutionLog records by any field
        distinct
            Filter ResolutionLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ResolutionLog
            The first ResolutionLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ResolutionLog record ordered by the rawResponse field
        resolutionlog = await ResolutionLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'rawResponse': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ResolutionLogUpdateInput,
        where: types.ResolutionLogWhereUniqueInput,
        include: Optional[types.ResolutionLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ResolutionLog record.

        Parameters
        ----------
        data
            ResolutionLog record data specifying what to update
        where
            ResolutionLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ResolutionLog model

        Returns
        -------
        prisma.models.ResolutionLog
            The updated ResolutionLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        resolutionlog = await ResolutionLog.prisma().update(
            where={
                'id': 'gifdddbia',
            },
            data={
                # data to update the ResolutionLog record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ResolutionLogWhereUniqueInput,
        data: types.ResolutionLogUpsertInput,
        include: Optional[types.ResolutionLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ResolutionLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ResolutionLog model

        Returns
        -------
        prisma.models.ResolutionLog
            The created or updated ResolutionLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        resolutionlog = await ResolutionLog.prisma().upsert(
            where={
                'id': 'bchehecef',
            },
            data={
                'create': {
                    'id': 'bchehecef',
                    'marketId': 'gaddfhfh',
                    'attemptNumber': 684462146,
                    'resolverType': 'bgcffadich',
                    'decision': 'fcbichhci',
                },
                'update': {
                    'marketId': 'gaddfhfh',
                    'attemptNumber': 684462146,
                    'resolverType': 'bgcffadich',
                    'decision': 'fcbichhci',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ResolutionLogUpdateManyMutationInput,
        where: types.ResolutionLogWhereInput,
    ) -> int:
        """Update multiple ResolutionLog records

        Parameters
        ----------
        data
            ResolutionLog data to update the selected ResolutionLog records to
        where
            Filter to select the ResolutionLog records to update

        Returns
        -------
        int
            The total number of ResolutionLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ResolutionLog records
        total = await ResolutionLog.prisma().update_many(
            data={
                'decision': 'jeijcbhfe'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ResolutionLogWhereInput] = None,
        cursor: Optional[types.ResolutionLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ResolutionLog records present in the database

        Parameters
        ----------
        select
            Select the ResolutionLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ResolutionLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ResolutionLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ResolutionLog.prisma().count()

        # results: prisma.types.ResolutionLogCountAggregateOutput
        results = await ResolutionLog.prisma().count(
            select={
                '_all': True,
                'reasoning': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ResolutionLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ResolutionLogWhereInput] = None,
        cursor: Optional[types.ResolutionLogWhereUniqueInput] = None,
    ) -> types.ResolutionLogCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ResolutionLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ResolutionLogWhereInput] = None,
        cursor: Optional[types.ResolutionLogWhereUniqueInput] = None,
    ) -> Union[int, types.ResolutionLogCountAggregateOutput]:
        """Count the number of ResolutionLog records present in the database

        Parameters
        ----------
        select
            Select the ResolutionLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ResolutionLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ResolutionLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ResolutionLog.prisma().count()

        # results: prisma.types.ResolutionLogCountAggregateOutput
        results = await ResolutionLog.prisma().count(
            select={
                '_all': True,
                'txHash': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ResolutionLogCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ResolutionLogWhereInput] = None
    ) -> int:
        """Delete multiple ResolutionLog records.

        Parameters
        ----------
        where
            Optional ResolutionLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ResolutionLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ResolutionLog records
        total = await ResolutionLog.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ResolutionLogScalarFieldKeys'],
        *,
        where: Optional['types.ResolutionLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ResolutionLogAvgAggregateInput'] = None,
        sum: Optional['types.ResolutionLogSumAggregateInput'] = None,
        min: Optional['types.ResolutionLogMinAggregateInput'] = None,
        max: Optional['types.ResolutionLogMaxAggregateInput'] = None,
        having: Optional['types.ResolutionLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ResolutionLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ResolutionLogScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ResolutionLogScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ResolutionLogGroupByOutput']:
        """Group ResolutionLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ResolutionLog fields to group records by
        where
            ResolutionLog filter to select records
        take
            Limit the maximum number of ResolutionLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ResolutionLogGroupByOutput]
            A list of dictionaries representing the ResolutionLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ResolutionLog records by blockNumber values
        # and count how many records are in each group
        results = await ResolutionLog.prisma().group_by(
            ['blockNumber'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AgentCycleActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AgentCycle]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await AgentCycle.prisma().query_raw(
            'SELECT * FROM AgentCycle WHERE id = $1',
            'bjgejjabff',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AgentCycle
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await AgentCycle.prisma().query_first(
            'SELECT * FROM AgentCycle WHERE cycleType = $1',
            'bcciijbibg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AgentCycleCreateInput,
        include: Optional[types.AgentCycleInclude] = None
    ) -> _PrismaModelT:
        """Create a new AgentCycle record.

        Parameters
        ----------
        data
            AgentCycle record data
        include
            Specifies which relations should be loaded on the returned AgentCycle model

        Returns
        -------
        prisma.models.AgentCycle
            The created AgentCycle record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AgentCycle record from just the required fields
        agentcycle = await AgentCycle.prisma().create(
            data={
                # data to create a AgentCycle record
                'cycleType': 'cffcachfd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AgentCycleCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AgentCycle records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AgentCycle record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await AgentCycle.prisma().create_many(
            data=[
                {
                    # data to create a AgentCycle record
                    'cycleType': 'bccdfhdigc',
                },
                {
                    # data to create a AgentCycle record
                    'cycleType': 'febcgjbfj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AgentCycleWhereUniqueInput,
        include: Optional[types.AgentCycleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AgentCycle record.

        Parameters
        ----------
        where
            AgentCycle filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AgentCycle model

        Returns
        -------
        prisma.models.AgentCycle
            The deleted AgentCycle record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        agentcycle = await AgentCycle.prisma().delete(
            where={
                'id': 'bageiegghg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AgentCycleWhereUniqueInput,
        include: Optional[types.AgentCycleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AgentCycle record.

        Parameters
        ----------
        where
            AgentCycle filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AgentCycle model

        Returns
        -------
        prisma.models.AgentCycle
            The found AgentCycle record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        agentcycle = await AgentCycle.prisma().find_unique(
            where={
                'id': 'faidicegb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AgentCycleWhereUniqueInput,
        include: Optional[types.AgentCycleInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AgentCycle record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AgentCycle filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AgentCycle model

        Returns
        -------
        prisma.models.AgentCycle
            The found AgentCycle record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        agentcycle = await AgentCycle.prisma().find_unique_or_raise(
            where={
                'id': 'bacecgfhbe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AgentCycleWhereInput] = None,
        cursor: Optional[types.AgentCycleWhereUniqueInput] = None,
        include: Optional[types.AgentCycleInclude] = None,
        order: Optional[Union[types.AgentCycleOrderByInput, List[types.AgentCycleOrderByInput]]] = None,
        distinct: Optional[List[types.AgentCycleScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AgentCycle records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AgentCycle records returned
        skip
            Ignore the first N results
        where
            AgentCycle filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AgentCycle model
        order
            Order the returned AgentCycle records by any field
        distinct
            Filter AgentCycle records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AgentCycle]
            The list of all AgentCycle records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AgentCycle records
        agentcycles = await AgentCycle.prisma().find_many(take=10)

        # find the first 5 AgentCycle records ordered by the startedAt field
        agentcycles = await AgentCycle.prisma().find_many(
            take=5,
            order={
                'startedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AgentCycleWhereInput] = None,
        cursor: Optional[types.AgentCycleWhereUniqueInput] = None,
        include: Optional[types.AgentCycleInclude] = None,
        order: Optional[Union[types.AgentCycleOrderByInput, List[types.AgentCycleOrderByInput]]] = None,
        distinct: Optional[List[types.AgentCycleScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AgentCycle record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AgentCycle filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AgentCycle model
        order
            Order the returned AgentCycle records by any field
        distinct
            Filter AgentCycle records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AgentCycle
            The first AgentCycle record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AgentCycle record ordered by the completedAt field
        agentcycle = await AgentCycle.prisma().find_first(
            skip=1,
            order={
                'completedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AgentCycleWhereInput] = None,
        cursor: Optional[types.AgentCycleWhereUniqueInput] = None,
        include: Optional[types.AgentCycleInclude] = None,
        order: Optional[Union[types.AgentCycleOrderByInput, List[types.AgentCycleOrderByInput]]] = None,
        distinct: Optional[List[types.AgentCycleScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AgentCycle record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AgentCycle filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AgentCycle model
        order
            Order the returned AgentCycle records by any field
        distinct
            Filter AgentCycle records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AgentCycle
            The first AgentCycle record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AgentCycle record ordered by the durationMs field
        agentcycle = await AgentCycle.prisma().find_first_or_raise(
            skip=1,
            order={
                'durationMs': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AgentCycleUpdateInput,
        where: types.AgentCycleWhereUniqueInput,
        include: Optional[types.AgentCycleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AgentCycle record.

        Parameters
        ----------
        data
            AgentCycle record data specifying what to update
        where
            AgentCycle filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AgentCycle model

        Returns
        -------
        prisma.models.AgentCycle
            The updated AgentCycle record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        agentcycle = await AgentCycle.prisma().update(
            where={
                'id': 'ihcahiead',
            },
            data={
                # data to update the AgentCycle record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AgentCycleWhereUniqueInput,
        data: types.AgentCycleUpsertInput,
        include: Optional[types.AgentCycleInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AgentCycle filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AgentCycle model

        Returns
        -------
        prisma.models.AgentCycle
            The created or updated AgentCycle record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        agentcycle = await AgentCycle.prisma().upsert(
            where={
                'id': 'biheheiajg',
            },
            data={
                'create': {
                    'id': 'biheheiajg',
                    'cycleType': 'febcgjbfj',
                },
                'update': {
                    'cycleType': 'febcgjbfj',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AgentCycleUpdateManyMutationInput,
        where: types.AgentCycleWhereInput,
    ) -> int:
        """Update multiple AgentCycle records

        Parameters
        ----------
        data
            AgentCycle data to update the selected AgentCycle records to
        where
            Filter to select the AgentCycle records to update

        Returns
        -------
        int
            The total number of AgentCycle records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AgentCycle records
        total = await AgentCycle.prisma().update_many(
            data={
                'prsAnalysed': 916896761
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AgentCycleWhereInput] = None,
        cursor: Optional[types.AgentCycleWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AgentCycle records present in the database

        Parameters
        ----------
        select
            Select the AgentCycle fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AgentCycle filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AgentCycleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AgentCycle.prisma().count()

        # results: prisma.types.AgentCycleCountAggregateOutput
        results = await AgentCycle.prisma().count(
            select={
                '_all': True,
                'marketsProposed': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AgentCycleCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AgentCycleWhereInput] = None,
        cursor: Optional[types.AgentCycleWhereUniqueInput] = None,
    ) -> types.AgentCycleCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AgentCycleCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AgentCycleWhereInput] = None,
        cursor: Optional[types.AgentCycleWhereUniqueInput] = None,
    ) -> Union[int, types.AgentCycleCountAggregateOutput]:
        """Count the number of AgentCycle records present in the database

        Parameters
        ----------
        select
            Select the AgentCycle fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AgentCycle filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AgentCycleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AgentCycle.prisma().count()

        # results: prisma.types.AgentCycleCountAggregateOutput
        results = await AgentCycle.prisma().count(
            select={
                '_all': True,
                'marketsDeployed': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AgentCycleCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AgentCycleWhereInput] = None
    ) -> int:
        """Delete multiple AgentCycle records.

        Parameters
        ----------
        where
            Optional AgentCycle filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AgentCycle records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AgentCycle records
        total = await AgentCycle.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AgentCycleScalarFieldKeys'],
        *,
        where: Optional['types.AgentCycleWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AgentCycleAvgAggregateInput'] = None,
        sum: Optional['types.AgentCycleSumAggregateInput'] = None,
        min: Optional['types.AgentCycleMinAggregateInput'] = None,
        max: Optional['types.AgentCycleMaxAggregateInput'] = None,
        having: Optional['types.AgentCycleScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AgentCycleCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AgentCycleScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AgentCycleScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AgentCycleGroupByOutput']:
        """Group AgentCycle records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AgentCycle fields to group records by
        where
            AgentCycle filter to select records
        take
            Limit the maximum number of AgentCycle records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AgentCycleGroupByOutput]
            A list of dictionaries representing the AgentCycle record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AgentCycle records by marketsResolved values
        # and count how many records are in each group
        results = await AgentCycle.prisma().group_by(
            ['marketsResolved'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SecurityAdvisoryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.SecurityAdvisory]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await SecurityAdvisory.prisma().query_raw(
            'SELECT * FROM SecurityAdvisory WHERE id = $1',
            'hgjcghfbi',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.SecurityAdvisory
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await SecurityAdvisory.prisma().query_first(
            'SELECT * FROM SecurityAdvisory WHERE cveId = $1',
            'icadbcehj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SecurityAdvisoryCreateInput,
        include: Optional[types.SecurityAdvisoryInclude] = None
    ) -> _PrismaModelT:
        """Create a new SecurityAdvisory record.

        Parameters
        ----------
        data
            SecurityAdvisory record data
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model

        Returns
        -------
        prisma.models.SecurityAdvisory
            The created SecurityAdvisory record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a SecurityAdvisory record from just the required fields
        securityadvisory = await SecurityAdvisory.prisma().create(
            data={
                # data to create a SecurityAdvisory record
                'severity': 'jchciaee',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SecurityAdvisoryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple SecurityAdvisory records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of SecurityAdvisory record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await SecurityAdvisory.prisma().create_many(
            data=[
                {
                    # data to create a SecurityAdvisory record
                    'severity': 'deeificjd',
                },
                {
                    # data to create a SecurityAdvisory record
                    'severity': 'bbcbhebbda',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SecurityAdvisoryWhereUniqueInput,
        include: Optional[types.SecurityAdvisoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single SecurityAdvisory record.

        Parameters
        ----------
        where
            SecurityAdvisory filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model

        Returns
        -------
        prisma.models.SecurityAdvisory
            The deleted SecurityAdvisory record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        securityadvisory = await SecurityAdvisory.prisma().delete(
            where={
                'id': 'bejfijgcfb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SecurityAdvisoryWhereUniqueInput,
        include: Optional[types.SecurityAdvisoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique SecurityAdvisory record.

        Parameters
        ----------
        where
            SecurityAdvisory filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model

        Returns
        -------
        prisma.models.SecurityAdvisory
            The found SecurityAdvisory record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        securityadvisory = await SecurityAdvisory.prisma().find_unique(
            where={
                'id': 'caifcbgii',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SecurityAdvisoryWhereUniqueInput,
        include: Optional[types.SecurityAdvisoryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique SecurityAdvisory record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            SecurityAdvisory filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model

        Returns
        -------
        prisma.models.SecurityAdvisory
            The found SecurityAdvisory record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        securityadvisory = await SecurityAdvisory.prisma().find_unique_or_raise(
            where={
                'id': 'igaibbfgj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecurityAdvisoryWhereInput] = None,
        cursor: Optional[types.SecurityAdvisoryWhereUniqueInput] = None,
        include: Optional[types.SecurityAdvisoryInclude] = None,
        order: Optional[Union[types.SecurityAdvisoryOrderByInput, List[types.SecurityAdvisoryOrderByInput]]] = None,
        distinct: Optional[List[types.SecurityAdvisoryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple SecurityAdvisory records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of SecurityAdvisory records returned
        skip
            Ignore the first N results
        where
            SecurityAdvisory filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model
        order
            Order the returned SecurityAdvisory records by any field
        distinct
            Filter SecurityAdvisory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.SecurityAdvisory]
            The list of all SecurityAdvisory records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 SecurityAdvisory records
        securityadvisorys = await SecurityAdvisory.prisma().find_many(take=10)

        # find the first 5 SecurityAdvisory records ordered by the ghsaId field
        securityadvisorys = await SecurityAdvisory.prisma().find_many(
            take=5,
            order={
                'ghsaId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SecurityAdvisoryWhereInput] = None,
        cursor: Optional[types.SecurityAdvisoryWhereUniqueInput] = None,
        include: Optional[types.SecurityAdvisoryInclude] = None,
        order: Optional[Union[types.SecurityAdvisoryOrderByInput, List[types.SecurityAdvisoryOrderByInput]]] = None,
        distinct: Optional[List[types.SecurityAdvisoryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single SecurityAdvisory record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SecurityAdvisory filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model
        order
            Order the returned SecurityAdvisory records by any field
        distinct
            Filter SecurityAdvisory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SecurityAdvisory
            The first SecurityAdvisory record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SecurityAdvisory record ordered by the severity field
        securityadvisory = await SecurityAdvisory.prisma().find_first(
            skip=1,
            order={
                'severity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SecurityAdvisoryWhereInput] = None,
        cursor: Optional[types.SecurityAdvisoryWhereUniqueInput] = None,
        include: Optional[types.SecurityAdvisoryInclude] = None,
        order: Optional[Union[types.SecurityAdvisoryOrderByInput, List[types.SecurityAdvisoryOrderByInput]]] = None,
        distinct: Optional[List[types.SecurityAdvisoryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single SecurityAdvisory record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SecurityAdvisory filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model
        order
            Order the returned SecurityAdvisory records by any field
        distinct
            Filter SecurityAdvisory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SecurityAdvisory
            The first SecurityAdvisory record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SecurityAdvisory record ordered by the packageName field
        securityadvisory = await SecurityAdvisory.prisma().find_first_or_raise(
            skip=1,
            order={
                'packageName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SecurityAdvisoryUpdateInput,
        where: types.SecurityAdvisoryWhereUniqueInput,
        include: Optional[types.SecurityAdvisoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single SecurityAdvisory record.

        Parameters
        ----------
        data
            SecurityAdvisory record data specifying what to update
        where
            SecurityAdvisory filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model

        Returns
        -------
        prisma.models.SecurityAdvisory
            The updated SecurityAdvisory record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        securityadvisory = await SecurityAdvisory.prisma().update(
            where={
                'id': 'bggajdcbbi',
            },
            data={
                # data to update the SecurityAdvisory record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SecurityAdvisoryWhereUniqueInput,
        data: types.SecurityAdvisoryUpsertInput,
        include: Optional[types.SecurityAdvisoryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            SecurityAdvisory filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned SecurityAdvisory model

        Returns
        -------
        prisma.models.SecurityAdvisory
            The created or updated SecurityAdvisory record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        securityadvisory = await SecurityAdvisory.prisma().upsert(
            where={
                'id': 'fcfhgbjed',
            },
            data={
                'create': {
                    'id': 'fcfhgbjed',
                    'severity': 'bbcbhebbda',
                },
                'update': {
                    'severity': 'bbcbhebbda',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SecurityAdvisoryUpdateManyMutationInput,
        where: types.SecurityAdvisoryWhereInput,
    ) -> int:
        """Update multiple SecurityAdvisory records

        Parameters
        ----------
        data
            SecurityAdvisory data to update the selected SecurityAdvisory records to
        where
            Filter to select the SecurityAdvisory records to update

        Returns
        -------
        int
            The total number of SecurityAdvisory records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all SecurityAdvisory records
        total = await SecurityAdvisory.prisma().update_many(
            data={
                'summary': 'hdgcajhjg'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecurityAdvisoryWhereInput] = None,
        cursor: Optional[types.SecurityAdvisoryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of SecurityAdvisory records present in the database

        Parameters
        ----------
        select
            Select the SecurityAdvisory fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SecurityAdvisory filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SecurityAdvisoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SecurityAdvisory.prisma().count()

        # results: prisma.types.SecurityAdvisoryCountAggregateOutput
        results = await SecurityAdvisory.prisma().count(
            select={
                '_all': True,
                'publishedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SecurityAdvisoryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecurityAdvisoryWhereInput] = None,
        cursor: Optional[types.SecurityAdvisoryWhereUniqueInput] = None,
    ) -> types.SecurityAdvisoryCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SecurityAdvisoryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SecurityAdvisoryWhereInput] = None,
        cursor: Optional[types.SecurityAdvisoryWhereUniqueInput] = None,
    ) -> Union[int, types.SecurityAdvisoryCountAggregateOutput]:
        """Count the number of SecurityAdvisory records present in the database

        Parameters
        ----------
        select
            Select the SecurityAdvisory fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SecurityAdvisory filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SecurityAdvisoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SecurityAdvisory.prisma().count()

        # results: prisma.types.SecurityAdvisoryCountAggregateOutput
        results = await SecurityAdvisory.prisma().count(
            select={
                '_all': True,
                'rawPayload': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SecurityAdvisoryCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SecurityAdvisoryWhereInput] = None
    ) -> int:
        """Delete multiple SecurityAdvisory records.

        Parameters
        ----------
        where
            Optional SecurityAdvisory filter to find the records to be deleted

        Returns
        -------
        int
            The total number of SecurityAdvisory records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all SecurityAdvisory records
        total = await SecurityAdvisory.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SecurityAdvisoryScalarFieldKeys'],
        *,
        where: Optional['types.SecurityAdvisoryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SecurityAdvisoryAvgAggregateInput'] = None,
        sum: Optional['types.SecurityAdvisorySumAggregateInput'] = None,
        min: Optional['types.SecurityAdvisoryMinAggregateInput'] = None,
        max: Optional['types.SecurityAdvisoryMaxAggregateInput'] = None,
        having: Optional['types.SecurityAdvisoryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SecurityAdvisoryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SecurityAdvisoryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SecurityAdvisoryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SecurityAdvisoryGroupByOutput']:
        """Group SecurityAdvisory records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar SecurityAdvisory fields to group records by
        where
            SecurityAdvisory filter to select records
        take
            Limit the maximum number of SecurityAdvisory records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SecurityAdvisoryGroupByOutput]
            A list of dictionaries representing the SecurityAdvisory record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group SecurityAdvisory records by fetchedAt values
        # and count how many records are in each group
        results = await SecurityAdvisory.prisma().group_by(
            ['fetchedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models