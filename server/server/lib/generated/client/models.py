# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Market(bases.BaseMarket):
    """Represents a Market record"""

    id: _str
    onchainMarketId: Optional[_int] = None
    transactionHash: Optional[_str] = None
    blockNumber: Optional[_int] = None
    contractAddress: Optional[_str] = None
    title: _str
    question: _str
    category: _str
    options: List[_str]
    agentReason: _str
    resolutionType: 'enums.ResolutionType'
    dataSourceUrl: _str
    evaluationLogic: 'fields.Json'
    sourcePrNumber: Optional[_int] = None
    sourcePrUrl: Optional[_str] = None
    tssScore: Optional[_float] = None
    status: 'enums.MarketStatus'
    outcome: 'enums.Outcome'
    resolvedAt: Optional[datetime.datetime] = None
    resolutionTxHash: Optional[_str] = None
    resolutionNote: Optional[_str] = None
    initialLiquidityEth: Optional[_float] = None
    resolutionDeadline: Optional[datetime.datetime] = None
    resolveAttempts: _int
    lastAttemptAt: Optional[datetime.datetime] = None
    nextRetryAt: Optional[datetime.datetime] = None
    lastError: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    resolutionLogs: Optional[List['models.ResolutionLog']] = None
    prRecord: Optional['models.DeployedPR'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('options', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.MarketKeys']] = None,
        exclude: Optional[Iterable['types.MarketKeys']] = None,
        required: Optional[Iterable['types.MarketKeys']] = None,
        optional: Optional[Iterable['types.MarketKeys']] = None,
        relations: Optional[Mapping['types.MarketRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.MarketKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Market_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Market_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Market_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Market_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Market_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Market_relational_fields:
                        raise errors.UnknownRelationalFieldError('Market', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Market / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Market',
            }
        )
        _created_partial_types.add(name)


class DeployedPR(bases.BaseDeployedPR):
    """Represents a DeployedPR record"""

    id: _str
    prNumber: _int
    prTitle: _str
    prUrl: Optional[_str] = None
    mergedAt: Optional[datetime.datetime] = None
    tssScore: Optional[_float] = None
    deployedAt: datetime.datetime
    marketId: Optional[_str] = None
    market: Optional['models.Market'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DeployedPRKeys']] = None,
        exclude: Optional[Iterable['types.DeployedPRKeys']] = None,
        required: Optional[Iterable['types.DeployedPRKeys']] = None,
        optional: Optional[Iterable['types.DeployedPRKeys']] = None,
        relations: Optional[Mapping['types.DeployedPRRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DeployedPRKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DeployedPR_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DeployedPR_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DeployedPR_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DeployedPR_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DeployedPR_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DeployedPR_relational_fields:
                        raise errors.UnknownRelationalFieldError('DeployedPR', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DeployedPR / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DeployedPR',
            }
        )
        _created_partial_types.add(name)


class ResolutionLog(bases.BaseResolutionLog):
    """Represents a ResolutionLog record"""

    id: _str
    marketId: _str
    market: Optional['models.Market'] = None
    attemptNumber: _int
    resolverType: _str
    rawResponse: Optional['fields.Json'] = None
    decision: _str
    reasoning: Optional[_str] = None
    txHash: Optional[_str] = None
    blockNumber: Optional[_int] = None
    error: Optional[_str] = None
    attemptedAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ResolutionLogKeys']] = None,
        exclude: Optional[Iterable['types.ResolutionLogKeys']] = None,
        required: Optional[Iterable['types.ResolutionLogKeys']] = None,
        optional: Optional[Iterable['types.ResolutionLogKeys']] = None,
        relations: Optional[Mapping['types.ResolutionLogRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ResolutionLogKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ResolutionLog_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ResolutionLog_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ResolutionLog_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ResolutionLog_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ResolutionLog_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ResolutionLog_relational_fields:
                        raise errors.UnknownRelationalFieldError('ResolutionLog', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ResolutionLog / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ResolutionLog',
            }
        )
        _created_partial_types.add(name)


class AgentCycle(bases.BaseAgentCycle):
    """Represents a AgentCycle record"""

    id: _str
    cycleType: _str
    startedAt: datetime.datetime
    completedAt: Optional[datetime.datetime] = None
    durationMs: Optional[_int] = None
    prsAnalysed: Optional[_int] = None
    marketsProposed: Optional[_int] = None
    marketsDeployed: Optional[_int] = None
    marketsResolved: Optional[_int] = None
    errors: List[_str]
    metadata: Optional['fields.Json'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('errors', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AgentCycleKeys']] = None,
        exclude: Optional[Iterable['types.AgentCycleKeys']] = None,
        required: Optional[Iterable['types.AgentCycleKeys']] = None,
        optional: Optional[Iterable['types.AgentCycleKeys']] = None,
        relations: Optional[Mapping['types.AgentCycleRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AgentCycleKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AgentCycle_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AgentCycle_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AgentCycle_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AgentCycle_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "AgentCycle" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AgentCycle / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AgentCycle',
            }
        )
        _created_partial_types.add(name)


class SecurityAdvisory(bases.BaseSecurityAdvisory):
    """Represents a SecurityAdvisory record"""

    id: _str
    cveId: Optional[_str] = None
    ghsaId: Optional[_str] = None
    severity: _str
    packageName: Optional[_str] = None
    summary: Optional[_str] = None
    publishedAt: Optional[datetime.datetime] = None
    rawPayload: Optional['fields.Json'] = None
    fetchedAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SecurityAdvisoryKeys']] = None,
        exclude: Optional[Iterable['types.SecurityAdvisoryKeys']] = None,
        required: Optional[Iterable['types.SecurityAdvisoryKeys']] = None,
        optional: Optional[Iterable['types.SecurityAdvisoryKeys']] = None,
        relations: Optional[Mapping['types.SecurityAdvisoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SecurityAdvisoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _SecurityAdvisory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _SecurityAdvisory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _SecurityAdvisory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _SecurityAdvisory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "SecurityAdvisory" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid SecurityAdvisory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'SecurityAdvisory',
            }
        )
        _created_partial_types.add(name)



_Market_relational_fields: Set[str] = {
        'resolutionLogs',
        'prRecord',
    }
_Market_fields: Dict['types.MarketKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('onchainMarketId', {
            'name': 'onchainMarketId',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionHash', {
            'name': 'transactionHash',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('blockNumber', {
            'name': 'blockNumber',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('contractAddress', {
            'name': 'contractAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('question', {
            'name': 'question',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('options', {
            'name': 'options',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('agentReason', {
            'name': 'agentReason',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolutionType', {
            'name': 'resolutionType',
            'is_list': False,
            'optional': False,
            'type': 'enums.ResolutionType',
            'is_relational': False,
            'documentation': None,
        }),
        ('dataSourceUrl', {
            'name': 'dataSourceUrl',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('evaluationLogic', {
            'name': 'evaluationLogic',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('sourcePrNumber', {
            'name': 'sourcePrNumber',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('sourcePrUrl', {
            'name': 'sourcePrUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('tssScore', {
            'name': 'tssScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.MarketStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('outcome', {
            'name': 'outcome',
            'is_list': False,
            'optional': False,
            'type': 'enums.Outcome',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolvedAt', {
            'name': 'resolvedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolutionTxHash', {
            'name': 'resolutionTxHash',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolutionNote', {
            'name': 'resolutionNote',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('initialLiquidityEth', {
            'name': 'initialLiquidityEth',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolutionDeadline', {
            'name': 'resolutionDeadline',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolveAttempts', {
            'name': 'resolveAttempts',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastAttemptAt', {
            'name': 'lastAttemptAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('nextRetryAt', {
            'name': 'nextRetryAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastError', {
            'name': 'lastError',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolutionLogs', {
            'name': 'resolutionLogs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ResolutionLog\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('prRecord', {
            'name': 'prRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.DeployedPR',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DeployedPR_relational_fields: Set[str] = {
        'market',
    }
_DeployedPR_fields: Dict['types.DeployedPRKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('prNumber', {
            'name': 'prNumber',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('prTitle', {
            'name': 'prTitle',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('prUrl', {
            'name': 'prUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('mergedAt', {
            'name': 'mergedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('tssScore', {
            'name': 'tssScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('deployedAt', {
            'name': 'deployedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('marketId', {
            'name': 'marketId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('market', {
            'name': 'market',
            'is_list': False,
            'optional': True,
            'type': 'models.Market',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ResolutionLog_relational_fields: Set[str] = {
        'market',
    }
_ResolutionLog_fields: Dict['types.ResolutionLogKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('marketId', {
            'name': 'marketId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('market', {
            'name': 'market',
            'is_list': False,
            'optional': True,
            'type': 'models.Market',
            'is_relational': True,
            'documentation': None,
        }),
        ('attemptNumber', {
            'name': 'attemptNumber',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('resolverType', {
            'name': 'resolverType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rawResponse', {
            'name': 'rawResponse',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('decision', {
            'name': 'decision',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('reasoning', {
            'name': 'reasoning',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('txHash', {
            'name': 'txHash',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('blockNumber', {
            'name': 'blockNumber',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('error', {
            'name': 'error',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('attemptedAt', {
            'name': 'attemptedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_AgentCycle_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_AgentCycle_fields: Dict['types.AgentCycleKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('cycleType', {
            'name': 'cycleType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('startedAt', {
            'name': 'startedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('completedAt', {
            'name': 'completedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('durationMs', {
            'name': 'durationMs',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('prsAnalysed', {
            'name': 'prsAnalysed',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('marketsProposed', {
            'name': 'marketsProposed',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('marketsDeployed', {
            'name': 'marketsDeployed',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('marketsResolved', {
            'name': 'marketsResolved',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('errors', {
            'name': 'errors',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('metadata', {
            'name': 'metadata',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_SecurityAdvisory_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_SecurityAdvisory_fields: Dict['types.SecurityAdvisoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('cveId', {
            'name': 'cveId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('ghsaId', {
            'name': 'ghsaId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('severity', {
            'name': 'severity',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('packageName', {
            'name': 'packageName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('summary', {
            'name': 'summary',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('publishedAt', {
            'name': 'publishedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('rawPayload', {
            'name': 'rawPayload',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('fetchedAt', {
            'name': 'fetchedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Market)
model_rebuild(DeployedPR)
model_rebuild(ResolutionLog)
model_rebuild(AgentCycle)
model_rebuild(SecurityAdvisory)
